---
const currentYear = new Date().getFullYear();
const navItems = [
  { label: 'Work', href: '/#work', labelIt: 'Lavori' },
  { label: 'About', href: '/#about', labelIt: 'Chi Sono' },
  { label: 'Contact', href: '/#contact', labelIt: 'Contatti' },
];

const currentPath = Astro.url.pathname;
const isItalian = currentPath.startsWith('/it');
---

<footer class="border-t border-cyan-100 mt-3xl" style="background-color: #f0f9fb;">
  <div class="max-w-content mx-auto px-6 md:px-20 py-xl md:py-2xl">
    <div class="flex flex-col md:flex-row justify-between items-center gap-6">
      <!-- Copyright -->
      <p class="text-body-sm text-gray-700 font-medium flex items-center gap-2">
        © {currentYear} Cris' Portfolio. Built with
        <a href="https://claude.ai" target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-1 text-cyan-700 hover:text-cyan-900 font-medium transition-colors">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
          </svg>
          Claude
        </a>
      </p>

      <!-- Footer Navigation -->
      <nav>
        <ul class="flex items-center gap-6">
          {navItems.map(item => (
            <li>
              <a
                href={isItalian ? `/it${item.href}` : item.href}
                class="text-body-sm text-gray-700 hover:text-cyan-700 font-medium transition-all"
              >
                {isItalian ? item.labelIt : item.label}
              </a>
            </li>
          ))}
        </ul>
      </nav>
    </div>
  </div>
</footer>
